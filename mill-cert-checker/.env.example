# Copy to .env and fill in a real key to switch extraction from mock to live (ADR-001, ADR-011).
# app.py currently reads this from the shell environment; loading .env automatically is a TODO.
ANTHROPIC_API_KEY=your-key-here
